# Light Remote MCP — Handoff 2026-09-10

Status: `v0.9.0-beta.1` source candidate on `codex/v0.9-device-policy-resume`. Owner approved Apache-2.0 and public beta promotion. Linux migration, Device Policy, Windows acceptance, signed rollback, and pre-beta distribution gate are closed locally; production `main` remains unchanged until CI promotion.

## Read first

1. `AI_BRIDGE_GUIDE.md`
2. `CURRENT_STATE.md`
3. `PRODUCT_PLATFORM_PLAN_V0_6_TO_PUBLIC_PLUGIN.md`
4. `FLEET_ROUTING_V0_8.md`
5. `PLATFORM_ADAPTERS_V0_9.md`
6. `DEVICE_POLICY_V0_9_ACCEPTANCE_2026-09-10.md`
7. `LINUX_SIGNED_UPDATE_V0_9_ACCEPTANCE_2026-09-10.md`
8. this handoff

Repository: `/home/ubuntu/n8n2erpnext/gpt-vps-bridge`
Branch: `codex/v0.9-device-policy-resume`
Previous committed checkpoint before this closure: `3deabf5 test(v0.9): clean maintenance fixture on failure`

## Live topology at closure

`ChatGPT -> Vercel -> ARM Hub -> {ARM local | VPS-AMD Linux x64 | DESKTOP-VA16D27 Windows x64}`

ARM and Windows report `0.9.0-dev`. AMD is intentionally left on signed acceptance fixture `0.9.0-rc.1`; this is not a public release or stable tag. All three nodes were repeatedly observed online with zero active sessions after acceptance cleanup.
## Linux signed updater closure

Structured Wall action `Run signed update now` is live. Backend/UI action opens a bounded maintenance session, submits the privileged updater start through the selected Linux leaf, and auto-closes the maintenance session after the job finishes. The reusable proof is `deploy/scripts/live-v09-maintenance-update-trigger.mjs`.

The AMD package layout is now root-owned and versioned under `/opt/gpt-operator-agent/releases`; `/opt/gpt-operator-agent/current` selects the active release. The updater timer is enabled/active. Post-acceptance release prep cut the updater back to the canonical GitHub Release manifest/signature URLs; the temporary NetBird fixture channel is no longer configured on AMD.

Two real updater defects were found and fixed before release:
- ESM main detection failed when systemd invoked the updater through the `current` symlink. `invokedAsMain()` now compares canonical real paths; regression executes the updater through a symlink.
- post-restart validation could accept a crash-looping release during a transient `systemctl is-active` window. The updater now requires `ActiveState=active`, `SubState=running`, non-zero MainPID, stable for the health window; rollback must also reach stable health.

Good-path live proof: signed `0.9.0-rc.1` moved AMD from `0.9.0-dev` to `rc.1`; journal recorded `updated:0.9.0-dev->0.9.0-rc.1`, `current` pointed to the rc.1 release, and the agent returned `active/running` with a real MainPID.

Rollback live proof: signed/hash-valid `0.9.0-rc.2` intentionally shipped a non-executable bundled Node runtime. Updater failed visibly with `update_failed:update_rollback:0.9.0-rc.2`, restored `current` to `rc.1`, and recovered the agent to stable `active/running`. Restoring signed rc.1 then produced `up_to_date:0.9.0-rc.1` and updater success.

No GitHub Release, stable channel, `main`, or tag was touched during acceptance.
## Final regression and soak

ARM full selftest sweep: `36/36` PASS, `0` skips, `0` failures. `git diff --check` must remain clean at commit time.

Fleet was sampled three consecutive rounds: ARM online, AMD online, Windows online, all with zero active sessions. AMD soak sampled the same healthy MainPID across three rounds with updater timer active and no drain state. Windows acceptance was rerun twice and passed filesystem, PowerShell, Git/bundled Node, process/network, Services, Event Log, and package-manager probes.

Device Policy remains revision 3 on AMD and Windows after restore. The earlier Windows package-manager deny/re-enable and AMD sudo-on-demand deny/re-enable proofs remain authoritative.

## Important operating boundary

Wall is no longer accurately described as strictly read-only. It is an authenticated owner control plane with narrow structured mutations: Device Policy and signed-update maintenance. It still does not expose a generic shell, does not own process/session lifetime, and all mutations remain policy-bounded/audited.

RDC remains rescue/failover for self-disruptive steps. Normal project operation should use the Light Remote MCP path once the carrying services are healthy.

## Next step

Do not redo v0.9 acceptance. Final diff/security review is now complete and documented in `V0_9_RELEASE_REVIEW_2026-09-10.md`. The temporary acceptance update channel has been removed from AMD and the ARM fixture listener stopped. Runtime redistribution notices are now package-contract requirements for Linux Node.js and Windows Node.js/.NET, with details in `THIRD_PARTY_DISTRIBUTION_NOTICES.md`. Notice-hardened package CI on commit `119850a` is green: Linux run `34485046439` PASS for x64+arm64 and Windows run `34485046414` PASS through installed-layout and rollback smoke. Promotion remains held until the owner explicitly approves `main`/release promotion and selects a project-wide root license.

## Beta continuation checkpoint
Owner selected Apache-2.0 and approved `v0.9.0-beta.1`. Root license/notice, public architecture roadmap, self-host Linux Server installer/builder, Vercel release bundle, beta release workflow, Windows self-host Server settings, Linux self-host endpoint flags, beta update channel, and release notes are in the working candidate.

Pre-commit local gate: 37/37 selftests PASS; root/gateway npm audit both 0; beta workflow YAML parse PASS; local arm64 Server bundle and Vercel bundle build/extract PASS. Windows prerelease ordering was fixed before release: custom SemVer comparison covers beta -> rc -> stable, and Windows self-test now fails if ordering regresses.

Next sequence: commit/push candidate branch -> require Linux client, Windows native, Linux Server and Vercel package CI green -> fast-forward/promote `main` -> deploy reference Vercel/ARM from promoted source -> tag `v0.9.0-beta.1` -> let tag workflow publish prerelease assets -> generate/sign beta client manifest with external signing key -> commit channel metadata -> migrate reference AMD from acceptance rc.1 to beta.1 explicitly (SemVer correctly refuses downgrade), update Windows and reference ARM/gateway -> final fleet soak.

## 2026-09-11 RDC replacement continuity update
RDC has a short remaining availability window, so Light Remote self-dogfood is now P0. Native owner OAuth for `/mcp` plus a self-describing remote-control tool surface has been implemented and live-dogfooded locally through the real ARM operator socket. The proof reaches ARM, AMD and Windows and exercises durable session/exec/job/output semantics.

Current local gate: 38/38 selftests PASS, OAuth fleet dogfood PASS, root/gateway npm audit 0. The next sequence is commit/push this OAuth batch, wait for package CI, deploy the committed gateway to the reference ARM path, run the same OAuth proof through the public `mcp.dashboard.thaiduy.store` endpoint, then connect a fresh ChatGPT custom MCP App and prove operation without RDC.
