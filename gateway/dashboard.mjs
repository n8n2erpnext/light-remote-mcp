import { brandFaviconSvg, brandTitleSvg } from './brand.mjs';

const MATERIAL_ICON_PATHS=Object.freeze({
  copy:'M360-240q-33 0-56.5-23.5T280-320v-480q0-33 23.5-56.5T360-880h360q33 0 56.5 23.5T800-800v480q0 33-23.5 56.5T720-240H360Zm0-80h360v-480H360v480ZM200-80q-33 0-56.5-23.5T120-160v-560h80v560h440v80H200Zm160-240v-480 480Z',
  pause:'M520-200v-560h240v560H520Zm-320 0v-560h240v560H200Zm400-80h80v-400h-80v400Zm-320 0h80v-400h-80v400Z',
  logout:'M202-160q-33 0-56.5-23.5T122-240v-480q0-33 23.5-56.5T202-800h240v80H202v480h240v80H202Zm408-160-56-58 62-62H362v-80h254l-62-62 56-58 160 160-160 160Z',
  security:'M480-80q-139-35-229.5-159.5T160-516v-244l320-120 320 120v244q0 152-90.5 276.5T480-80Zm0-84q104-33 172-132t68-220v-189l-240-90-240 90v189q0 121 68 220t172 132Z'
});
function materialIcon(name){const path=MATERIAL_ICON_PATHS[name];if(!path)throw new Error('unknown_material_icon:'+name);return `<svg class="mi" viewBox="0 -960 960 960" aria-hidden="true" focusable="false"><path d="${path}"/></svg>`;}

export function dashboardHtml(options={}) {
  const fleet=String(options.surface||'hosted')==='fleet';
  const pageTitle=fleet?'Light Remote MCP — Fleet Wall':'Light Remote MCP — Operator Wall';
  const subtitle=fleet?'Fleet Wall · Main device · multi-device observability':'operator wall · ARM hub · session lanes · full history on VPS disk';
  const showLogout=options.showLogout!==false;
  const csrfToken=String(options.csrfToken||'');
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">${brandFaviconSvg()}
<title>${pageTitle}</title><meta name="robots" content="noindex,nofollow,noarchive"><style>
@font-face{font-family:'Cascadia Mono';src:url('/assets/fonts/CascadiaMono.ttf') format('truetype');font-style:normal;font-weight:200 700;font-display:swap}:root{color-scheme:dark;font-family:'Cascadia Mono',ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;background:#080a0c;color:#d8dee7}*{box-sizing:border-box}body{margin:0;background:#080a0c}.wrap{max-width:1500px;margin:auto;padding:16px}.top{display:flex;justify-content:space-between;gap:12px;flex-wrap:wrap;align-items:center}.muted{color:#718096}.pill,.btn,input,select,.tab{border:1px solid #2b333d;background:#0e1216;color:#cfd7e3;border-radius:7px;padding:7px 9px;font:inherit}.btn{display:inline-flex;align-items:center;justify-content:center;gap:6px;text-decoration:none;cursor:pointer}.mi{width:16px;height:16px;fill:currentColor;flex:0 0 auto}.brand-title{display:flex;align-items:center;gap:11px}.brand-title>span{display:flex;align-items:baseline;gap:7px}.brand-title strong{font-size:19px;color:#f3f4f6}.brand-title small{font-size:10px;letter-spacing:.16em;color:#718096}.brand-mark{flex:0 0 auto}.ok{color:#78d78b}.hold,.running{color:#e6c36b}.error{color:#ff8e8e}.timeout{color:#ffb56b}.controls,.tabs{display:flex;gap:7px;flex-wrap:wrap}.tabs{margin-top:12px;overflow:auto;flex-wrap:nowrap;padding-bottom:3px}.tab{cursor:pointer;white-space:nowrap}.tab.active{background:#1a222b;border-color:#61758a}.tab .state{margin-left:6px;font-size:11px}.sessionbox{margin-top:9px;border:1px solid #202832;background:#0a0e12;border-radius:8px;padding:9px 11px;font-size:12px;line-height:1.5}.term{margin-top:10px;border:1px solid #252d36;border-radius:10px;background:#050708;overflow:hidden}.head{padding:9px 12px;border-bottom:1px solid #20262d;display:flex;justify-content:space-between;gap:10px}#jobs{max-height:78vh;overflow:auto;padding:8px}.job{border:1px solid #171d23;border-radius:8px;margin:7px 0;background:#090c0f}.job.hidden{display:none}.commandrow{display:grid;grid-template-columns:70px 82px minmax(220px,1fr) auto;gap:10px;padding:10px;align-items:start;border-bottom:1px solid #151a20}.cmdwrap{display:flex;gap:8px;align-items:flex-start;flex-wrap:wrap;min-width:0}.opbadge{border:1px solid #40505f;background:#111922;color:#9fd0ff;border-radius:999px;padding:2px 7px;font-size:10px;font-weight:800;letter-spacing:.05em;white-space:nowrap;margin-top:2px}.opbadge.op-fs{border-color:#236b7a;background:#0b2228;color:#67e8f9}.opbadge.op-search{border-color:#246b46;background:#0c2117;color:#75e6a4}.opbadge.op-runtime{border-color:#7a5723;background:#24190a;color:#f3bd61}.riskbadge{border-radius:999px;padding:2px 7px;font-size:10px;font-weight:900;letter-spacing:.05em;white-space:nowrap;margin-top:2px}.risk-yellow{border:1px solid #8b741f;background:#2a2308;color:#ffe066}.risk-red{border:1px solid #8f3232;background:#2a0c0c;color:#ff8e8e}.cmdtext{color:#CCCCCC;font-size:13.5px;line-height:1.45;white-space:pre-wrap;word-break:break-word;min-width:0}.syn-command{color:#16C60C;font-weight:700}.syn-danger-command{color:#E74856;font-weight:800}.syn-option{color:#61D6D6}.syn-string{color:#F9F1A5}.syn-path{color:#3B78FF}.syn-var{color:#B4009E}.syn-op{color:#F2F2F2;font-weight:700}.syn-keyword{color:#B4009E}.syn-comment{color:#767676;font-style:italic}.syn-number{color:#3A96DD}.resultline{display:block;color:#9fb0c2;font-size:11px;line-height:1.45;margin-top:4px}.meta{padding:8px 10px;color:#8390a1;font-size:12px;white-space:pre-wrap}pre{margin:0;padding:10px;white-space:pre-wrap;word-break:break-word;font-size:12px;line-height:1.45;max-height:46vh;overflow:auto}.stderr{color:#ffb0b0}.actions,.topright{display:flex;gap:6px;align-items:center;flex-wrap:wrap}.topright form{margin:0}.small{font-size:11px;padding:5px 7px}@media(max-width:800px){.wrap{padding:9px}.commandrow{grid-template-columns:64px 1fr}.cmdtext{grid-column:1/-1}.actions{grid-column:1/-1}.meta{font-size:11px}}
</style></head><body><div class="wrap"><div class="top"><div>${brandTitleSvg(44)}<div class="muted" style="margin-top:6px">${subtitle}</div></div><div class="topright"><span id="conn" class="pill">connecting…</span>${showLogout?'<form method="post" action="/auth/logout"><input type="hidden" name="csrf" value="'+csrfToken+'"><button class="btn small" type="submit">'+materialIcon('logout')+'<span>Logout</span></button></form>':''}</div></div>
<div id="devicebox" class="sessionbox muted">loading devices…</div>
<div id="tabs" class="tabs"></div><div id="sessionbox" class="sessionbox muted">loading sessions…</div>
<div class="controls" style="margin-top:9px"><input id="filter" placeholder="filter cwd / command / output / PTY"><select id="status"><option value="">all status</option><option>running</option><option>ok</option><option>error</option><option>timeout</option></select><button id="pause" class="btn">${materialIcon('pause')}<span>Pause</span></button><button id="copyVisible" class="btn">${materialIcon('copy')}<span>Copy visible</span></button></div>
<div class="term"><div class="head"><span>LIVE OPERATOR STREAM</span><span id="stats" class="muted">0 jobs</span></div><div id="jobs"></div></div>
<script>
const box=document.getElementById('jobs'),devicebox=document.getElementById('devicebox'),tabs=document.getElementById('tabs'),sessionbox=document.getElementById('sessionbox'),filter=document.getElementById('filter'),statusSel=document.getElementById('status'),conn=document.getElementById('conn'),stats=document.getElementById('stats');
const surfaceFleet=${fleet?'true':'false'};
if(surfaceFleet){const settings=document.createElement('a');settings.className='btn small';settings.href='/settings';settings.textContent='Settings';document.querySelector('.topright')?.prepend(settings);}
const jobs=new Map(),sessions=new Map(),seenEvents=new Set(),seenOrder=[],dirtyJobs=new Set(),pendingKeys=new Set();
let paused=false,pending=[],selected='ALL',deviceRows=[],renderTimer=null,sessionRefreshTimer=null,sessionsInFlight=null,devicesInFlight=null,probeInFlight=false,catchupInFlight=false;
function esc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));}
function short(v,n=10){v=String(v||'');return v.length>n?v.slice(0,n)+'…':v;}
function fmtRemaining(ms){ms=Math.max(0,Number(ms)||0);const m=Math.floor(ms/60000),h=Math.floor(m/60);return h?(h+'h '+(m%60)+'m'):(m+'m');}
const LEGACY_BINARY_MARKER='[binary output suppressed · legacy event]'+String.fromCharCode(10);function safeOutputChunk(value){const s=String(value??'');if(!s)return '';let replacements=0,controls=0;for(const ch of s){const code=ch.charCodeAt(0);if(ch==='�')replacements++;else if(code<32&&code!==9&&code!==10&&code!==13)controls++;}if(replacements>=4||(replacements>=2&&replacements*50>s.length)||controls>=4)return LEGACY_BINARY_MARKER;return s;}function eventKey(e){return e&&e.id!=null&&e.at?String(e.id)+'@'+String(e.at):'';}
function eventSeen(e){const k=eventKey(e);return !!k&&seenEvents.has(k);}
function rememberEvent(e){const k=eventKey(e);if(!k||seenEvents.has(k))return;seenEvents.add(k);seenOrder.push(k);while(seenOrder.length>12000)seenEvents.delete(seenOrder.shift());}
function renderDevices(){if(!deviceRows.length){devicebox.textContent='no registered devices';return;}devicebox.innerHTML=deviceRows.map(d=>{const c=d.connection||{},u=d.update||{},connected=c.state==='connected',cloud='<span class="'+(connected?'ok':'hold')+'">cloud '+esc(c.state||'dormant')+'</span>',remaining=connected&&c.remainingMs!=null?' · lease '+esc(fmtRemaining(c.remainingMs)):'',updateNote=u.state==='available'?' · <span class="running">Update → '+esc(u.targetVersion||'available')+'</span>':(['rollback','failed','core_healthy_helper_old'].includes(u.state)?' · <span class="error">Update '+esc(u.state)+(u.code?' '+esc(u.code):'')+'</span>':'');return '<b>'+esc(d.displayName||d.deviceId)+'</b> · <span class="'+esc(d.state==='online'?'ok':'muted')+'">'+esc(d.state||'unknown')+'</span> · '+cloud+remaining+' · '+esc(d.platform||'unknown')+'/'+esc(d.architecture||'unknown')+' · sessions '+esc(d.activeSessions||0)+' · agent '+esc(d.agentVersion||'unknown')+updateNote+(d.policy?' · <a class="btn small" href="/device-policy?id='+encodeURIComponent(d.deviceId)+'">${materialIcon('security')}<span>Policy</span></a>':'' );}).join('<br>');}
function latencyText(j){const l=j.latency||{},pairs=[['bridge',l.bridgeMs],['gateway',l.gatewayMs],['dispatch',l.dispatchMs],['first-byte',l.firstByteMs],['execution',l.executionMs],['wall',j.wallLatencyMs]];return pairs.filter(([,v])=>Number.isFinite(v)).map(([k,v])=>k+' '+Math.round(v)+'ms').join(' · ');}
function markWallDisplay(j){j.wallDisplayedAt=Date.now();const source=Date.parse(j.lastEventAt||j.at||'');j.wallLatencyMs=Number.isFinite(source)?Math.max(0,j.wallDisplayedAt-source):null;}
function fmtBytes(n){n=Math.max(0,Number(n)||0);if(n<1024)return n+' B';if(n<1024*1024)return (n/1024).toFixed(n<10*1024?1:0)+' KB';return (n/1024/1024).toFixed(n<10*1024*1024?1:0)+' MB';}function fsCommand(j){const m=j.toolMeta||{};if(m.kind!=='fs')return '';const op=String(m.op||'unknown'),head='fs.'+op;if(op==='readMany'){const paths=Array.isArray(m.paths)?m.paths:[],count=Number(m.pathCount||paths.length);return head+' '+count+' path'+(count===1?'':'s')+(paths[0]?' · '+String(paths[0]):'')+(count>1?' · +'+(count-1):'');}if(op==='copy'||op==='move')return head+' '+String(m.source||'')+' -> '+String(m.destination||'')+(m.overwrite?' · overwrite':'');const p=String(m.path||'');let extra='';if(op==='write')extra=' · '+fmtBytes(m.bytes)+' · '+String(m.mode||'rewrite');else if(op==='edit')extra=' · '+fmtBytes(m.oldBytes)+' -> '+fmtBytes(m.newBytes)+(m.expectedReplacements!=null?' · expect '+m.expectedReplacements:'');else if(op==='read'){if(m.startLine!=null)extra+=' · line '+m.startLine;if(m.maxLines!=null)extra+=' · max '+m.maxLines+' lines';if(m.tailLines!=null)extra+=' · tail '+m.tailLines;}else if(op==='list'&&m.maxDepth!=null)extra=' · depth '+m.maxDepth;else if(op==='mkdir'&&m.parents)extra=' · parents';else if(op==='delete'&&m.recursive)extra=' · recursive';return head+(p?' '+p:'')+extra;}const LEGACY_TOOL_PREFIXES=[['native-fs:','fs'],['native-search:','search'],['native-process:','process'],['native-terminal:','terminal'],['native-desktop:','desktop'],['light-scp:','scp']];function toolIdentity(j){const m=j.toolMeta||{};let kind=String(m.kind||'').toLowerCase(),op=String(m.op||'').toLowerCase();if(!kind){for(const raw of [String(j.note||''),String(j.script||'')]){for(const [prefix,name] of LEGACY_TOOL_PREFIXES){if(raw.startsWith(prefix)){kind=name;op=raw.slice(prefix.length).toLowerCase();break;}}if(kind)break;}}return{m,kind,op:op||'unknown'};}function syntheticToolScript(j){const script=String(j.script||'');return LEGACY_TOOL_PREFIXES.some(([prefix])=>script.startsWith(prefix));}function nativeHandle(v){v=String(v||'');return v.length>18?v.slice(0,18)+'…':v;}function toolCommand(j){const i=toolIdentity(j),m=i.m,op=i.op;if(i.kind==='fs')return fsCommand(j)||('fs.'+op+(j.cwd?' '+j.cwd:''));if(i.kind==='terminal')return String(m.label||('pty.'+op));if(i.kind==='search'){const q=m.pattern?JSON.stringify(String(m.pattern)):'',loc=String(m.path||j.cwd||''),file=String(m.filePattern||'');if(op==='start')return ['search.start',String(m.searchType||'content'),q,file,loc].filter(Boolean).join(' · ');if(op==='results')return ['search.results',q,file,loc,!q&&!loc?nativeHandle(m.searchId):'',Number(m.offset)>0?'offset '+m.offset:''].filter(Boolean).join(' · ');if(op==='cancel')return ['search.cancel',q,file,loc,!q&&!loc?nativeHandle(m.searchId):''].filter(Boolean).join(' · ');return 'search.'+op;}if(i.kind==='process'){if(op==='input')return 'process.input '+nativeHandle(m.processId)+' · '+String(m.inputBytes||0)+' B'+(m.eof?' · EOF':'');if(op==='output')return 'process.output '+nativeHandle(m.processId)+' · '+String(m.stream||'stdout')+' · offset '+String(m.offset||0)+' · limit '+String(m.limit||262144)+' B';if(op==='stop')return 'process.stop '+nativeHandle(m.processId)+(m.force?' · force':'');if(op==='list')return 'process.list';return 'process.'+op+(j.cwd?' '+j.cwd:'');}if(i.kind==='scp'){const id=nativeHandle(m.transferId);if(op==='upload-begin')return ['scp.upload',String(m.destination||''),m.totalBytes!=null?fmtBytes(m.totalBytes):'',m.overwrite?'overwrite':''].filter(Boolean).join(' · ');if(op==='download-begin')return ['scp.download',String(m.source||''),m.totalBytes!=null?fmtBytes(m.totalBytes):''].filter(Boolean).join(' · ');if(op==='upload-chunk'||op==='download-chunk')return ['scp.'+op,id,m.index!=null?'chunk '+m.index:'',m.offset!=null?'offset '+m.offset:'',m.bytes!=null?fmtBytes(m.bytes):''].filter(Boolean).join(' · ');if(op==='status'||op==='cancel'||op==='upload-commit')return 'scp.'+op+' '+id;return 'scp.'+op;}if(i.kind==='desktop')return String(m.label||('desktop.'+op));return i.kind?(i.kind+'.'+op):'';}function commandText(j){const formatted=toolCommand(j),script=String(j.script||'');if(formatted&&(!script||syntheticToolScript(j)))return String(formatted);return String(script||formatted||j.note||j.jobId||'');}function toolLabel(j){const i=toolIdentity(j),names={terminal:'PTY',fs:'FS',search:'SEARCH',process:'PROCESS',scp:'SCP',desktop:'DESKTOP'},name=names[i.kind]||String(i.kind||'').toUpperCase();return name?(name+' '+String(i.op||'').toUpperCase()):'';}function groupBadgeClass(j){const k=toolIdentity(j).kind;return k==='fs'?'op-fs':k==='search'?'op-search':['process','terminal','scp','desktop'].includes(k)?'op-runtime':'';}function riskBadge(j){const i=toolIdentity(j),m=i.m||{},op=i.op,caps=(Array.isArray(j.requiredCapabilities)?j.requiredCapabilities:[]).map(v=>String(v).toLowerCase()),text=(String(j.script||'')+' '+String(j.note||'')+' '+commandText(j)).toLowerCase();let danger=caps.includes('sudo-on-demand')||caps.includes('systemctl')||['sudo ','systemctl ','shutdown','reboot','poweroff','mkfs','rm -rf','rm -fr','git reset --hard','git push --force','git push -f','maintenance','signed client update'].some(v=>text.includes(v));if(i.kind==='fs'&&(op==='delete'||m.overwrite===true))danger=true;if(i.kind==='process'&&op==='stop'&&m.force===true)danger=true;if(danger)return{label:'DANGER',cls:'risk-red'};let mutate=caps.some(c=>['docker','lxd','build-test'].includes(c))||['git add ','git commit ','git push ','git checkout ','git switch ','git merge ','git rebase ','git cherry-pick ','git tag ','mv ','cp ','mkdir ','touch ','chmod ','chown ','install ','tee ','sed -i'].some(v=>text.includes(v));if(i.kind==='fs'&&['write','edit','mkdir','copy','move'].includes(op))mutate=true;if(i.kind==='process'&&['start','input','stop'].includes(op))mutate=true;if(i.kind==='terminal'&&['start','input','resize','signal','stop'].includes(op))mutate=true;if(i.kind==='scp'&&(op.startsWith('upload')||op==='cancel'))mutate=true;if(i.kind==='desktop'&&op==='input')mutate=true;if(!mutate)return null;const system=caps.some(c=>['docker','lxd','build-test'].includes(c));return{label:system?'SYSTEM':'MUTATE',cls:'risk-yellow'};}function shellEsc(v){return String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#39;');}const SHELL_KEYWORDS=new Set(['if','then','elif','else','fi','for','foreach','in','do','done','while','until','case','esac','function','select','switch','trap','return','break','continue','export','readonly','local','declare','typeset','set','unset']);const SHELL_WRAPPERS=new Set(['sudo','env','command','builtin','exec','nohup','time','xargs']);const SHELL_DANGER=new Set(['sudo','systemctl','shutdown','reboot','poweroff','mkfs','rm']);function shellSyntaxHtml(value){const text=String(value??''),bs=String.fromCharCode(92),tab=String.fromCharCode(9),lf=String.fromCharCode(10),cr=String.fromCharCode(13),bt=String.fromCharCode(96);let out='',i=0,expectCommand=true;const emit=(cls,t)=>{out+=cls?'<span class="'+cls+'">'+shellEsc(t)+'</span>':shellEsc(t);};const space=c=>c===' '||c===tab||c===lf||c===cr;const opchar=c=>c==='|'||c==='&'||c===';'||c==='>'||c==='<';const digitWord=t=>{if(!t)return false;for(const c of t)if(c<'0'||c>'9')return false;return true;};const pathWord=t=>t.startsWith('./')||t.startsWith('../')||t.startsWith('/')||t.startsWith('~/')||t.includes(bs)||(t.length>2&&t[1]===':'&&(t[2]===bs||t[2]==='/'));while(i<text.length){const c=text[i];if(space(c)){let j=i+1;while(j<text.length&&space(text[j]))j++;const t=text.slice(i,j);emit('',t);if(t.includes(lf)||t.includes(cr))expectCommand=true;i=j;continue;}if(c==='#'&&(i===0||space(text[i-1]))){let j=i+1;while(j<text.length&&text[j]!==lf&&text[j]!==cr)j++;emit('syn-comment',text.slice(i,j));i=j;continue;}if(c==="'"||c==='"'||c===bt){const q=c;let j=i+1;while(j<text.length){if(text[j]===q){j++;break;}if(text.charCodeAt(j)===92&&j+1<text.length)j+=2;else j++;}emit('syn-string',text.slice(i,j));expectCommand=false;i=j;continue;}if(c==='$'){let j=i+1;while(j<text.length&&!space(text[j])&&!opchar(text[j])&&text[j]!=="'"&&text[j]!=='"'&&text[j]!==bt)j++;emit('syn-var',text.slice(i,j));expectCommand=false;i=j;continue;}if(c==='%'){const end=text.indexOf('%',i+1);if(end>i+1){emit('syn-var',text.slice(i,end+1));expectCommand=false;i=end+1;continue;}}if(opchar(c)){let j=i+1;while(j<text.length&&opchar(text[j]))j++;const t=text.slice(i,j);emit('syn-op',t);if(t.includes('|')||t.includes('&')||t.includes(';'))expectCommand=true;i=j;continue;}let j=i+1;while(j<text.length&&!space(text[j])&&!opchar(text[j])&&text[j]!=="'"&&text[j]!=='"'&&text[j]!==bt)j++;const t=text.slice(i,j),low=t.toLowerCase();let cls='';if(t.startsWith('-'))cls='syn-option';else if(t.includes('=')&&!pathWord(t))cls='syn-var';else if(pathWord(t))cls='syn-path';else if(SHELL_KEYWORDS.has(low)){cls='syn-keyword';expectCommand=['if','then','elif','else','for','foreach','while','until','do','case','function','select','switch'].includes(low);}else if(expectCommand){cls=SHELL_DANGER.has(low)?'syn-danger-command':'syn-command';expectCommand=SHELL_WRAPPERS.has(low);}else if(digitWord(t))cls='syn-number';emit(cls,t);i=j;}return out;}function terminalHandle(j){const id=String(j.toolMeta?.terminalId||'');return id?(id.length>18?id.slice(0,18)+'…':id):'';}function jobText(j){return ['['+(j.status||'running')+'] '+j.jobId,toolLabel(j)?'operation: '+toolLabel(j):'',terminalHandle(j)?'terminal: '+terminalHandle(j):'','node: '+(j.nodeId||'arm'),'session: '+(j.sessionId||''),'agent: '+(j.agentId||''),'request: '+(j.requestId||''),'cwd: '+(j.cwd||''),'command:\\n'+commandText(j),j.resultSummary?'result: '+j.resultSummary:'',j.stdout?'stdout:\\n'+j.stdout:'',j.stderr?'stderr:\\n'+j.stderr:'',j.exitCode!=null?'exit: '+j.exitCode:'',j.durationMs!=null?'duration_ms: '+j.durationMs:'',latencyText(j)?'latency: '+latencyText(j):''].filter(Boolean).join('\\n');}
function liveSessions(){return [...sessions.values()].filter(s=>s.state==='active'||s.state==='hold').sort((a,b)=>a.createdAt-b.createdAt);}
function nodeKey(nodeId){return 'NODE:'+String(nodeId||'arm');}
function selectedNodeId(){return selected.startsWith('NODE:')?selected.slice(5):null;}function nodeLabel(nodeId){const id=String(nodeId||'arm'),device=deviceRows.find(d=>(d.nodeId||'arm')===id);return device?.displayName||id.toUpperCase();}
function renderTabs(){const live=liveSessions(),nodes=[...new Set([...deviceRows.map(d=>d.nodeId||'arm'),...live.map(s=>s.nodeId||'arm')])].sort();if(selected!=='ALL'&&!live.some(s=>s.sessionId===selected)&&!nodes.some(n=>nodeKey(n)===selected))selected='ALL';tabs.innerHTML='';const all=document.createElement('button');all.className='tab '+(selected==='ALL'?'active':'');all.textContent='ALL · '+live.length;all.onclick=()=>{selected='ALL';renderTabs();applyFilter();};tabs.appendChild(all);for(const n of nodes){const b=document.createElement('button'),count=live.filter(s=>(s.nodeId||'arm')===n).length;b.className='tab '+(selected===nodeKey(n)?'active':'');b.textContent=nodeLabel(n)+' · '+count;b.onclick=()=>{selected=nodeKey(n);renderTabs();applyFilter();};tabs.appendChild(b);}for(const s of live){const b=document.createElement('button');b.className='tab '+(selected===s.sessionId?'active':'');b.innerHTML=esc(nodeLabel(s.nodeId||'arm')+' · '+(s.label||short(s.sessionId,12)))+' <span class="state '+esc(s.state)+'">'+esc(s.state)+'</span>';b.onclick=()=>{selected=s.sessionId;renderTabs();applyFilter();};tabs.appendChild(b);}renderSessionBox();}
function renderSessionBox(){if(selected==='ALL'){const byNode={};for(const s of liveSessions())byNode[s.nodeId||'arm']=(byNode[s.nodeId||'arm']||0)+1;sessionbox.textContent=(surfaceFleet?'fleet':'hub ARM')+' · live sessions '+liveSessions().length+' · '+Object.entries(byNode).map(([n,c])=>nodeLabel(n)+' '+c).join(' · ')+' · target 1–5';return;}const nodeId=selectedNodeId();if(nodeId){const live=liveSessions().filter(s=>(s.nodeId||'arm')===nodeId),device=deviceRows.find(d=>(d.nodeId||'arm')===nodeId),state=device?.state||'unknown';sessionbox.innerHTML='<b>'+esc(nodeLabel(nodeId))+'</b> · <span class="'+esc(state==='online'?'ok':'muted')+'">'+esc(state)+'</span><br>live sessions '+esc(live.length)+' · device '+esc(device?.displayName||device?.deviceId||'unregistered')+' · '+esc(device?.platform||'unknown')+'/'+esc(device?.architecture||'unknown');return;}const s=sessions.get(selected);if(!s){sessionbox.textContent='session unavailable';return;}const graceMin=(s.graceMs||s.leaseMs)?Math.round((s.graceMs||s.leaseMs)/60000):0;sessionbox.innerHTML='<b>'+esc(s.label||s.sessionId)+'</b> · '+esc(nodeLabel(s.nodeId||'arm'))+' · '+esc(s.state)+'<br>session '+esc(s.sessionId)+' · agent '+esc(short(s.agentId,18))+' · workspace '+esc(s.workspace||'')+'<br>reconnect grace '+esc(s.gracePreset||s.leasePreset||'default')+(graceMin?' · '+esc(graceMin)+'m':'')+' · calls '+esc(s.stats?.toolCalls||0)+' · exec '+esc(s.stats?.execCalls||0)+' · active jobs '+esc(s.activeJobs?.length||0)+' · reconnects '+esc(s.reconnectCount||0);}
function applyFilter(){const q=filter.value.toLowerCase(),st=statusSel.value,nodeId=selectedNodeId();let visible=0;for(const [id,j] of jobs){const el=document.getElementById('j-'+id);if(!el)continue;const lane=selected==='ALL'||(nodeId?(j.nodeId||'arm')===nodeId:j.sessionId===selected);const hay=q?[j.cwd,commandText(j),j.script,j.stdout,j.stderr,j.note,j.resultSummary,JSON.stringify(j.toolMeta||{}),toolLabel(j),terminalHandle(j),id,j.sessionId,j.agentId,j.nodeId].join(' ').toLowerCase():'';const hide=!lane||(q&&!hay.includes(q))||(st&&j.status!==st);el.classList.toggle('hidden',!!hide);if(!hide)visible++;}stats.textContent=visible+' visible / '+jobs.size+' jobs';}
function render(j){markWallDisplay(j);let el=document.getElementById('j-'+j.jobId);if(!el){el=document.createElement('div');el.className='job';el.id='j-'+j.jobId;box.prepend(el);while(box.children.length>2500)box.lastChild.remove();}const op=toolLabel(j),handle=terminalHandle(j),summary=j.resultSummary?'<span class="resultline">'+esc(j.resultSummary)+'</span>':'',badge=op?'<span class="opbadge '+esc(groupBadgeClass(j))+'">'+esc(op)+'</span>':'',risk=riskBadge(j),riskHtml=risk?'<span class=\"riskbadge '+esc(risk.cls)+'\">'+esc(risk.label)+'</span>':'',metaTerminal=handle?' · terminal '+esc(handle):'';el.innerHTML='<div class="commandrow"><span class="'+esc(j.status||'running')+'">'+esc(j.status||'running')+'</span><span class="muted">'+esc(new Date(j.at||j.startedAt||Date.now()).toLocaleTimeString())+'</span><div class="cmdwrap">'+badge+riskHtml+'<span><code class="cmdtext">'+shellSyntaxHtml(commandText(j))+'</code>'+summary+'</span></div><span class="actions"><button class="btn small" data-copy="cmd">${materialIcon('copy')}<span>Copy cmd</span></button><button class="btn small" data-copy="out">${materialIcon('copy')}<span>Copy output</span></button><button class="btn small" data-copy="raw">${materialIcon('copy')}<span>Copy raw</span></button></span></div><div class="meta">'+esc(nodeLabel(j.nodeId||'arm'))+' · session '+esc(j.sessionId||'')+' · agent '+esc(short(j.agentId,18))+metaTerminal+(j.cwd?' · '+esc(j.cwd):'')+(latencyText(j)?'<br><span class="muted">'+esc(latencyText(j))+'</span>':'')+'</div>'+(j.stdout?'<pre>'+esc(j.stdout)+'</pre>':'')+(j.stderr?'<pre class="stderr">'+esc(j.stderr)+'</pre>':'');el.querySelector('[data-copy="cmd"]').onclick=()=>navigator.clipboard.writeText(commandText(j));el.querySelector('[data-copy="out"]').onclick=()=>navigator.clipboard.writeText((j.stdout||'')+(j.stderr?'\\n[stderr]\\n'+j.stderr:''));el.querySelector('[data-copy="raw"]').onclick=()=>navigator.clipboard.writeText(jobText(j));}
function flushRenders(){renderTimer=null;const ids=[...dirtyJobs];dirtyJobs.clear();for(const id of ids){const j=jobs.get(id);if(j)render(j);}applyFilter();}
function scheduleRender(id){if(id)dirtyJobs.add(id);if(renderTimer)return;renderTimer=setTimeout(flushRenders,75);}
function scheduleSessionRefresh(delay=150){if(sessionRefreshTimer)return;sessionRefreshTimer=setTimeout(()=>{sessionRefreshTimer=null;refreshSessions();},delay);}
function ingest(e,opts={}){const queue=opts.queue!==false,doRender=opts.render!==false,refresh=opts.refresh!==false;if(!e||eventSeen(e))return false;const k=eventKey(e);if(paused&&queue){if(!k||!pendingKeys.has(k)){pending.push(e);if(k)pendingKeys.add(k);}return false;}let j=e.jobId?jobs.get(e.jobId):null;if(e.type==='job_started'){if(!j){j={jobId:e.jobId,requestId:e.requestId,sessionId:e.sessionId,agentId:e.agentId,nodeId:e.nodeId||'arm',status:'running',cwd:e.cwd,script:e.script,note:e.note,toolMeta:e.toolMeta||null,requiredCapabilities:e.requiredCapabilities||[],resultSummary:e.resultSummary||'',stdout:'',stderr:'',at:e.at};jobs.set(e.jobId,j);}else{Object.assign(j,{requestId:e.requestId||j.requestId,sessionId:e.sessionId||j.sessionId,agentId:e.agentId||j.agentId,nodeId:e.nodeId||j.nodeId,cwd:e.cwd??j.cwd,script:e.script??j.script,note:e.note??j.note,toolMeta:e.toolMeta||j.toolMeta||null,requiredCapabilities:e.requiredCapabilities||j.requiredCapabilities||[],resultSummary:e.resultSummary||j.resultSummary||'',at:e.at||j.at});}}else if(e.type==='stdout'||e.type==='stderr'){if(!j)return false;const chunk=safeOutputChunk(e.chunk);if(chunk&&!(chunk===LEGACY_BINARY_MARKER&&j[e.type].endsWith(LEGACY_BINARY_MARKER)))j[e.type]+=chunk;}else if(e.type==='job_finished'){if(!j){j={jobId:e.jobId,requestId:e.requestId,sessionId:e.sessionId,agentId:e.agentId,nodeId:e.nodeId||'arm',status:e.status||'ok',cwd:e.cwd||'',script:e.script||'',note:e.note||'',toolMeta:e.toolMeta||null,requiredCapabilities:e.requiredCapabilities||[],resultSummary:e.resultSummary||'',stdout:'',stderr:'',at:e.at};jobs.set(e.jobId,j);}Object.assign(j,{status:e.status,exitCode:e.exitCode,durationMs:e.durationMs,cwd:e.cwd??j.cwd,script:e.script??j.script,note:e.note??j.note,requiredCapabilities:e.requiredCapabilities||j.requiredCapabilities||[],resultSummary:e.resultSummary||j.resultSummary||'',toolMeta:e.toolMeta||j.toolMeta||null,latency:e.latency||j.latency});}else if(e.type&&e.type.startsWith('device_')){refreshDevices();rememberEvent(e);return true;}else if(e.type&&e.type.startsWith('session_')){if(refresh)scheduleSessionRefresh();rememberEvent(e);return true;}else{rememberEvent(e);return true;}if(j){j.lastEventAt=e.at||j.lastEventAt;if(e.latency)j.latency=e.latency;}rememberEvent(e);if(doRender&&j)scheduleRender(j.jobId);return true;}
async function refreshDevices(){if(devicesInFlight)return devicesInFlight;devicesInFlight=(async()=>{try{const r=await fetch('/api/devices',{cache:'no-store'}),d=await r.json();deviceRows=(Array.isArray(d.devices)?d.devices:[]).filter(row=>row&&row.state!=='revoked');renderDevices();renderTabs();applyFilter();}catch{devicebox.textContent='device metadata unavailable';}finally{devicesInFlight=null;}})();return devicesInFlight;}
async function refreshSessions(){if(sessionsInFlight)return sessionsInFlight;sessionsInFlight=(async()=>{try{const r=await fetch('/api/sessions',{cache:'no-store'}),d=await r.json();sessions.clear();for(const s of (d.sessions||[]))sessions.set(s.sessionId,s);renderTabs();applyFilter();}catch{sessionbox.textContent='session metadata unavailable';}finally{sessionsInFlight=null;}})();return sessionsInFlight;}
async function loadActivity(limit=5000){try{const r=await fetch('/api/activity?limit='+limit,{cache:'no-store'}),d=await r.json();for(const e of (d.events||[]))ingest(e,{queue:false,render:true,refresh:false});scheduleSessionRefresh(0);}catch{}}
async function catchUpActivity(){if(catchupInFlight)return catchupInFlight;catchupInFlight=(async()=>{try{const r=await fetch('/api/activity?limit=500',{cache:'no-store'}),d=await r.json();let touchedSession=false;for(const e of (d.events||[])){const wasSession=!!(e.type&&e.type.startsWith('session_'));if(ingest(e,{queue:true,render:true,refresh:false})&&wasSession)touchedSession=true;}if(touchedSession)scheduleSessionRefresh();}catch{}finally{catchupInFlight=null;}})();return catchupInFlight;}
async function probeActivityHead(){if(document.hidden||paused||probeInFlight)return;probeInFlight=true;try{const r=await fetch('/api/activity?limit=1',{cache:'no-store'}),d=await r.json(),head=(d.events||[])[0];if(head&&!eventSeen(head))await catchUpActivity();}catch{}finally{probeInFlight=false;}}
let es=null,sseLive=false;
function openEvents(){if(es||document.hidden)return;es=new EventSource('/events');es.onopen=()=>{sseLive=true;conn.textContent='live';conn.className='pill ok';};es.onerror=()=>{sseLive=false;conn.textContent='reconnecting';conn.className='pill running';};es.addEventListener('activity',ev=>{try{ingest(JSON.parse(ev.data));}catch{}});}
function closeEvents(){if(es){es.close();es=null;}sseLive=false;}
document.addEventListener('visibilitychange',()=>{if(document.hidden){closeEvents();return;}catchUpActivity();refreshDevices();refreshSessions();openEvents();});
openEvents();refreshDevices();refreshSessions();loadActivity();setInterval(()=>{if(!sseLive)probeActivityHead();},5000);setInterval(()=>{if(!document.hidden)refreshDevices();},30000);
filter.oninput=applyFilter;statusSel.onchange=applyFilter;
document.getElementById('pause').onclick=e=>{paused=!paused;e.currentTarget.querySelector('span').textContent=paused?'Resume':'Pause';if(!paused){const q=pending;pending=[];pendingKeys.clear();for(const item of q)ingest(item,{queue:false});probeActivityHead();}};
document.getElementById('copyVisible').onclick=()=>navigator.clipboard.writeText([...jobs.values()].filter(j=>{const el=document.getElementById('j-'+j.jobId);return el&&!el.classList.contains('hidden');}).map(jobText).join('\\n\\n---\\n\\n'));

</script></div></body></html>`;
}
