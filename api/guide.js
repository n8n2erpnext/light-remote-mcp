module.exports = function handler(req, res) {
  res.setHeader('Cache-Control','no-store'); res.setHeader('X-Robots-Tag','noindex,nofollow,noarchive');
  if(req.method!=='GET') return res.status(405).json({ok:false,error:'method_not_allowed'});
  return res.status(200).json({ok:true,project:'gpt-vps-bridge',version:'0.7.0-dev',purpose:'ChatGPT-facing relay to the owner ARM operator hub',
    activePath:'ChatGPT -> Vercel bridge -> ARM hub -> selected node executor',upstream:'https://mcp.dashboard.thaiduy.store',wall:'https://wall.dashboard.thaiduy.store',
    recoveryDocs:['CURRENT_STATE.md','AI_BRIDGE_GUIDE.md','SESSION_OWNERSHIP_V0_5.md','HUB_TOPOLOGY_V0_5.md','PRODUCT_PLATFORM_PLAN_V0_6_TO_PUBLIC_PLUGIN.md','DEVICE_PRESENCE_V0_6.md','DEVICE_ENROLLMENT_V0_7.md','PORTABILITY.md'],
    auth:{login:'POST /api/auth body={username,password}',sessionHeader:'x-bridge-session',defaultTtlMinutes:15,staticSharedBearer:false,future:'hosted account/device OAuth + ChatGPT permission plane'},
    deviceEnrollment:{agentLogin:'node device-agent/operator-agent.mjs login',codeTtlMinutes:10,codeInActivationUrl:false,pollTokenBits:256,keyAlgorithm:'Ed25519',approvedDeviceStarts:'offline',onlineRequiresSignedHeartbeat:true,localFinalDenyBoundary:true,linuxDaemon:'device-agent/install-linux-service.sh',remoteLeafExecution:'v0.8'},
    sessionStart:{agentId:'generate one stable random agent id for this ChatGPT agent/chat, e.g. a_<uuid>',openId:'stable id for one open attempt',open:'POST /api/operator body={action:"session-open",payload:{agentId,openId,label,workspace,leasePreset?,leaseMs?}}'},
    operatorEndpoints:{capabilities:'capabilities',devices:'devices',device:'device',enrollmentBegin:'POST action=enrollment-begin payload={publicIdentityKey,displayName,platform,architecture,agentVersion,fingerprintSummary,capabilities,policyProfile}',enrollmentPoll:'POST action=enrollment-poll payload={enrollmentId,pollToken}',enrollments:'action=enrollments',enrollmentCancel:'POST action=enrollment-cancel payload={enrollmentId}',enrollmentApprove:'POST action=enrollment-approve payload={code,displayName,approvedCapabilities,policyProfile}',deviceHeartbeat:'POST action=device-heartbeat payload={deviceId,timestamp,nonce,capabilities,signature}',deviceRevoke:'POST action=device-revoke payload={deviceId,reason}',sessionResume:'session-resume',sessionClose:'session-close',session:'session',sessions:'sessions',sessionStats:'session-stats',exec:'POST action=exec payload={operationId,script,cwd,timeoutMs,waitMs,sessionId,agentId,note}',job:'job',output:'output'},
    readRule:'Protected owner calls require a short-lived x-bridge-session. Device enrollment begin/poll and signed heartbeat are bootstrap/proof lanes authenticated downstream by Vercel OIDC plus device-code/poll-token/key proof as applicable.',
    sessionPolicy:{oneAgentOneLiveSession:true,ownerMismatch:'409 session_owner_mismatch',defaultLeaseMinutes:30,leasePresets:{'30m':30,'1h':60,'3h':180,custom:'leaseMs'},alwaysKeepAlive:'future-only',leaseMinMinutes:5,leaseMaxHours:24,activeJobHold:true,postJobGraceUsesSessionLease:true,maxActiveSessions:5,sessionHistoryDays:7,currentJobTimeoutHours:2},
    deviceModel:{presenceIndependentFromSessions:true,currentDevice:'arm-local',currentNode:'arm',presenceTtlSeconds:90,heartbeatSeconds:30,persistentRegistry:true,enrollment:'v0.7 candidate',remoteRouting:'v0.8'},
    rules:['One agent owns exactly one live operator session lane.','Device approval never implies online; signed key proof is required.','Approved capabilities cannot exceed device-requested capabilities and device-local deny may reduce them further.','Device private keys remain local; device code is one-time and not stored plaintext.','No static shared Bearer secret is used.','Use Git/worktree discipline for repo concurrency.','Never put passwords/tokens/private keys/cookies/device codes in URL payloads.','Structured POST is required for payload-bearing operator/enrollment actions.'],
    security:{bridgeCaller:'short-lived ARM-validated bridge session for owner calls',deviceIdentity:'Ed25519 public-key binding + signed heartbeat',upstreamCaller:'Vercel OIDC',envelope:'X25519 + HKDF-SHA256 + AES-256-GCM',replayProtection:true,semanticIdempotency:true,wallAuth:'independent local auth; NetBird optional outer defense',executorUser:'ubuntu'}
  });
};
